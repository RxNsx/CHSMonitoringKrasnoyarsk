worker_processes 4;

events { worker_connections 1024; }


http {
	server {
		location / {
			proxy_pass http://monitoring_client_container:3000;
		}
		location /api/ {
			proxy_pass http://monitoring_service_container:8080;
		}
	}
}
